import { FC } from "react";
import "./Header.scss";

const Header: FC = () => {
  return (
    <div className="header">
      <h1 className="header__title">Header</h1>
    </div>
  );
};
export default Header;
